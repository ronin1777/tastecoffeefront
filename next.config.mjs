/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "localhost",
      "127.0.0.1",
      "tastecoffee.liara.run",
      "tastecoffee.darkube.app",
    ],
  },
};

export default nextConfig;
